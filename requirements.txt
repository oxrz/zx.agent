# === Core dependencies ===
sounddevice>=0.5.0
soundfile>=0.12.0
numpy>=1.24.0
pyyaml>=6.0

# === Audio loopback capture (meeting/video mode, captures system output) ===
soundcard>=0.4.3

# === Speech recognition (STT) ===
# WebSocket client for the ASR POC server (Zipformer + OpenVINO Whisper on rzp)
websocket-client>=1.6.0

# === AI / LLM ===
httpx>=0.27.0

# === Process priority optimization (Windows) ===
psutil>=5.9.0

# === Optional: text-to-speech ===
# edge-tts>=6.0.0
# gtts>=2.3.0

# === Optional: hotkey wake ===
# keyboard>=0.13.5

# === Optional: transparent overlay GUI (gui/app.py) ===
PyQt6==6.10.2                # cross-platform GUI toolkit, only needed to run `python -m gui.app`
